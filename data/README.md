# data/

Place curriculum CSV files here (not committed).

Suggested:
- `data/criterios_primaria_tercer_ciclo.csv`

Expected headers can look like:
- `Curso;Área;ID Criterio (Área.Curso.Bloque.Núm);Criterio de evaluación;Descriptor operativo. Competencias Clave`

This repo includes scripts under `logic/` to parse/seed these into Firestore.
